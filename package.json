{
    "name": "useless-token",
    "version": "1.0.0",
    "description": "Useless ERC-20 token",
    "main": "",
    "scripts": {
        "compile": "npx hardhat compile",
        "deploy": "npx hardhat run scripts/deploy.js --network ropsten",
        "test": "npx hardhat test --network hardhat",
        "eslint": "npx eslint . --ignore-path .gitignore",
        "solhint": "npx solhint contracts/**/*.sol",
        "prettier": "npx prettier --write . --ignore-path .gitignore"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/therandomchoice/useless-token"
    },
    "keywords": [
        "useless",
        "erc-20"
    ],
    "author": "randomchoice",
    "license": "ISC",
    "dependencies": {
        "@openzeppelin/contracts": "^4.4.1"
    },
    "devDependencies": {
        "@nomiclabs/hardhat-ethers": "^2.0.3",
        "chai": "^4.3.4",
        "dotenv": "^10.0.0",
        "eslint": "^8.7.0",
        "ethers": "^5.5.2",
        "hardhat": "^2.8.0",
        "prettier": "^2.5.1",
        "prettier-plugin-solidity": "^1.0.0-beta.19",
        "solhint": "^3.3.6"
    }
}
